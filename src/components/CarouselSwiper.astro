---
import "swiper/css/bundle";
import { Image } from "astro:assets";
const { images, alt } = Astro.props;
---

<div class="swiper">
  <div class="swiper-wrapper">
    {
      images.map((img, i) => (
        <div class="swiper-slide">
          <Image src={img} alt={`${alt} ${i + 1}`} />
        </div>
      ))
    }
  </div>
  <div class="swiper-pagination"></div>
</div>

<style>
  .swiper {
    width: 100%;
    height: 250px;
    position: relative;
  }

  .swiper-slide {
    text-align: center;
    font-size: 18px;
    background: #fff;
    display: flex;
    justify-content: center;
    align-items: center;
  }

  .swiper-slide img {
    display: block;
    width: 100%;
    height: 100%;
    object-fit: cover;
  }
</style>

<style is:global>
  .swiper-pagination-bullet {
    background-color: var(--primary-color-tint);
    opacity: 0.75;
    width: 10px;
    height: 10px;
  }

  .swiper-pagination-bullet-active {
    background-color: var(--accent-color);
    opacity: 1;
  }
</style>

<script>
  import Swiper from "swiper/bundle";

  let slidesPerView = 1;
  if (window.innerWidth > 500) slidesPerView = 3;

  const swiper = new Swiper(".swiper", {
    slidesPerView: slidesPerView,
    spaceBetween: 24,
    pagination: {
      el: ".swiper-pagination",
      clickable: true,
    },
  });
  console.log(swiper);
</script>



<!-- 
---
import "swiper/css/bundle";
import { Image } from "astro:assets";
const { images, alt, slidesPerView = 1 } = Astro.props;
---

<div class="swiper">
  <div class="swiper-wrapper">
    {images.map((img, i) => (
      <div class="swiper-slide">
        <Image src={img} alt={`${alt} ${i + 1}`} />
      </div>
    ))}
  </div>
  <div class="swiper-pagination"></div>
</div>

<style>
  .swiper {
    width: 100%;
    height: 250px;
    position: relative;
  }

  .swiper-slide {
    text-align: center;
    font-size: 18px;
    background: #fff;
    display: flex;
    justify-content: center;
    align-items: center;
  }

  .swiper-slide img {
    display: block;
    width: 100%;
    height: 100%;
    object-fit: cover;
  }
</style>

<style is:global>
  .swiper-pagination-bullet {
    background-color: var(--primary-color-tint);
    opacity: 0.75;
    width: 10px;
    height: 10px;
  }

  .swiper-pagination-bullet-active {
    background-color: var(--accent-color);
    opacity: 1;
  }
</style>

<script>
  import Swiper from "swiper/bundle";

  const swiper = new Swiper(".swiper", {
    slidesPerView: Astro.props.slidesPerView,
    spaceBetween: 24,
    pagination: {
      el: ".swiper-pagination",
      clickable: true,
    },
  });

  console.log(swiper);
</script> -->
